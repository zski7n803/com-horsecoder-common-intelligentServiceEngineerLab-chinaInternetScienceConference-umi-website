

export type Setter = {
  // 会议通知列表设置
  setMeetingNotificationList: React.Dispatch<React.SetStateAction<any[]>>;
  // 相关下载列表设置
  setRelatedDownloadList: React.Dispatch<React.SetStateAction<any[]>>;
};