"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _defineProperty2 = _interopRequireDefault(require("@babel/runtime/helpers/defineProperty"));
// https://github.com/mrdoob/three.js/blob/master/src/core/Clock.js
class Clock {
  constructor(autoStart = true) {
    (0, _defineProperty2.default)(this, "autoStart", void 0);
    (0, _defineProperty2.default)(this, "startTime", 0);
    (0, _defineProperty2.default)(this, "oldTime", 0);
    (0, _defineProperty2.default)(this, "running", false);
    (0, _defineProperty2.default)(this, "elapsedTime", 0);
    this.autoStart = autoStart;
  }
  start() {
    this.startTime = (typeof performance === 'undefined' ? Date : performance).now();
    this.oldTime = this.startTime;
    this.elapsedTime = 0;
    this.running = true;
  }
  stop() {
    this.getElapsedTime();
    this.running = false;
    this.autoStart = false;
  }
  getElapsedTime() {
    this.getDelta();
    return this.elapsedTime;
  }
  getDelta() {
    let diff = 0;
    if (this.autoStart && !this.running) {
      this.start();
      return 0;
    }
    if (this.running) {
      const newTime = (typeof performance === 'undefined' ? Date : performance).now();
      diff = (newTime - this.oldTime) / 1000;
      this.oldTime = newTime;
      this.elapsedTime += diff;
    }
    return diff;
  }
}
exports.default = Clock;