/**
 * MapboxService
 */
import type { IMercator } from '@antv/l7-core';
import { Map } from '@antv/l7-map';
import Viewport from '../lib/web-mercator-viewport';
import BaseMapService from '../utils/BaseMapService';
export default class DefaultMapService extends BaseMapService<Map> {
    version: string;
    /**
     * 将经纬度转成墨卡托坐标
     * @param lnglat
     * @returns
     */
    lngLatToCoord(lnglat: [number, number], origin?: IMercator): [number, number];
    lngLatToMercator(lnglat: [number, number], altitude: number): IMercator;
    getModelMatrix(lnglat: [number, number], altitude: number, rotate: [number, number, number], scale?: [number, number, number], origin?: IMercator): number[];
    viewport: Viewport;
    init(): Promise<void>;
    protected creatMapContainer(id: string | HTMLDivElement): HTMLDivElement;
    exportMap(type: 'jpg' | 'png'): string;
    setMapStyle(style: any): void;
    getCanvasOverlays(): HTMLElement | null;
}
